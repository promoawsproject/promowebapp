import { SparklineDirective } from './sparkline.directive';

describe('SparklineDirective', () => {
  it('should create an instance', () => {
    const directive = new SparklineDirective();
    expect(directive).toBeTruthy();
  });
});
